export default function ArtImageTile({art}) {
    console.log(art)
    return (
        <div>
            {/* {galleries.map(gallery => {
                // console.log('HERE', gallery.objects)
               return <img src={`${gallery.url}`} />
            })} */}
        </div>
    )
}